# RSS Alerter

Checks an RSS feed periodically and alerts a webhook on new items

## Setup

sqlite3 db.sqlite < create_table.sql

docker run -it -e RSS=<rss feed> -e WEBHOOK=<webhook> -v $(pwd)/db.sqlite:/app/db.sqlite spiffytech/rss-alerter
